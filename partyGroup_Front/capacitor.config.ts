import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'partyGroup_Front',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
